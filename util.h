#pragma once

#include <vector>
#include <fstream>
#include <sstream>
#include <string>

using namespace std;

// Used to delimit
namespace Util
{
	vector<string> SplitString(const string &s, char delim);
};

